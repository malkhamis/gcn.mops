#include <Rdefines.h>

/* cnmops.cpp */
SEXP cnmops(SEXP xS, SEXP IS, SEXP covS, SEXP cycS, SEXP alphaInitS, SEXP lambdaInitS, SEXP alphaPriorS);

/* segment.cpp */
SEXP segment(SEXP xS, SEXP epsS, SEXP deltaS, SEXP maxIntS, SEXP minSegS, SEXP squashingS, SEXP cyberWeightS);

